import {connect} from'react-redux'

function Navbar() {
    return(
        <Navbar>
           Navbar
        </Navbar>
    )
}

const mapStateToProps = (state) => ({

})

export default connect(mapStateToProps,{
    
})(Navbar)