import {connect} from'react-redux'

function Footer() {
    return(
        <nav>
           Footer
        </nav>
    )
}

const mapStateToProps = (state) => ({

})

export default connect(mapStateToProps,{
    
})(Footer)