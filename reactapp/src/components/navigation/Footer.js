import {connect} from'react-redux'

function Footer() {
    return(
        <div>
           Footer
        </div>
    )
}

const mapStateToProps = state => ({

})

export default connect(mapStateToProps,{
    
})(Footer)